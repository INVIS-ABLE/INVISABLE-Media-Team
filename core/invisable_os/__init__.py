"""INVISABLE® AI Media Agency OS.

A unified, founder-led media operating system. Every engine in this package reads
from and writes back to a shared memory (``INVISABLE_BRAIN``) so that the platform
compounds what it learns. The whole system is governed by a single rule:

    If a decision increases reach but damages trust, reject it.
    If a decision increases awareness, trust, community value, and founder
    recognition simultaneously, prioritise it.

That rule is enforced in :mod:`invisable_os.guardrails`, not merely documented.
"""

__version__ = "0.1.0"
