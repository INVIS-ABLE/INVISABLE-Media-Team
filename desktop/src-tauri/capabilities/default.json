{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the INVISABLE Media desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:webview:allow-create-webview-window",
    "shell:allow-open",
    "opener:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
